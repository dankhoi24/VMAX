[project]
name = "vmax"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
]

[project.optional-dependencies]
dev = [
    "httpx2>=0.28",
]
dtb = [
    "pylibfdt>=1.7.2",
]

[tool.uv]
package = false
